using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_position : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //print("pos = " + transform.position);
	}
	
	// Update is called once per frame
	void Update () {
        print("pos = " + transform.position);
    }
}
